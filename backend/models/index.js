const User = require('./User');
const Dataset = require('./Dataset');
const TrainingModel = require('./TrainingModel');
const DetectionLog = require('./DetectionLog');

module.exports = {
  User,
  Dataset,
  TrainingModel,
  DetectionLog
};
